
- if @feature.errors.any?
  %ul
    - @feature.errors.full_messages.each do |message|
      %li= message


= simple_form_for [:admin, @feature] do |f|

  = f.input :title, label: 'タイトル'
  = f.input :status, as: :radio_buttons, label: '公開状態'
  = f.input :content, label: '本文'

  = f.button :submit

