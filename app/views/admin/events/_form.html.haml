
- if @event.errors.any?
  %ul
    - @event.errors.full_messages.each do |message|
      %li= message


= simple_form_for [:admin, @event] do |f|

  = f.input :title
  = f.input :content, label: 'Description for event'

  = f.button :submit

