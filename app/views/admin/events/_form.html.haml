
- if @event.errors.any?
  %ul
    - @event.errors.full_messages.each do |message|
      %li= message


= simple_form_for [:admin, @event] do |f|

  = f.input :title, label: 'タイトル'
  = f.input :content, label: 'イベント概要'

  = f.button :submit

